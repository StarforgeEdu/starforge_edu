MINIO_ROOT_USER=GENERATE_A_NON_DEFAULT_ROOT_USER
MINIO_ROOT_PASSWORD=GENERATE_A_LONG_RANDOM_PASSWORD
# Exact frontend origins only; the storage bootstrap rejects wildcards or drift.
MINIO_API_CORS_ALLOW_ORIGIN=https://app.example.com
